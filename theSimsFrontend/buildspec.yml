version: 0.2

phases:
  pre_build:
    commands:
      - ls
      - cd theSimsFrontend && yarn install
  build:
    commands:
      - cd theSimsFrontend &&yarn build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build